name: Completed Torn Trade Analyzer handoff record

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Torn Trade Analyzer repository handoff completed; workflow intentionally disabled.'
